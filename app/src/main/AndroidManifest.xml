<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.osvaldoairon.suasnotas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconemaior"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
                   android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CadActivity" 
                   android:screenOrientation="portrait"/>
        <activity android:name=".LimpActivity" 
                   android:screenOrientation="portrait"/>
        <activity android:name=".InfActivity"
                   android:screenOrientation="portrait"/>
        <activity android:name=".SigaaActivity" 
                   android:screenOrientation="portrait" />
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>
